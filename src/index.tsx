import { createRoot } from 'react-dom/client';
import { DisplayGoods } from './App';

// eslint-disable-next-line max-len
createRoot(document.getElementById('root') as HTMLElement).render(<DisplayGoods />);
