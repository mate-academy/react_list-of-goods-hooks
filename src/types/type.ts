export type Options = {
  value: number,
  label: string,
};

export type Props = {
  goods: string[] | [],
  isReversed: boolean,
  sorted: boolean,
  lengthSort: boolean,
  choosedLength: number,
};
